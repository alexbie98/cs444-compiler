#include "Weeder.h"
#include <vector>
#include <iostream>

using namespace std;

void weed(ParseTreeNode *t)
{
}

// vector<TokenType> getModifiersFromModifiersOpt(ParseTreeNode *modifiersOpt){
//     assert(modifiersOpt->symbol == MODIFIERS_OPT);
//     if modifiersOpt->c
// }

// vector<TokenType> getAllModifiers(ParseTreeNode *modOpt){
//     //vector<TokenType> modifiers;

//     //ParseTreeNode *current = modOpt;
//     for (ParseTreeNode* child : current->children){
//         if (child->symbol == MODIFIER){
//             assert(child->children[0]->token != NULL);
//             modifiers.push_back(child->children[0]->token[0].first);
//         }
//         else if (child->symbol == MODIFIERS){

//         }
//     }

// }
